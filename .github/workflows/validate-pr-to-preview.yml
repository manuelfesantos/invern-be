name: Validate PR to Preview

on:
  pull_request:
    branches:
      - preview

jobs:
  quality-gate:
    # lint + test + type-check (shared with validate-pr-to-main)
    uses: ./.github/workflows/quality-gate.yml
