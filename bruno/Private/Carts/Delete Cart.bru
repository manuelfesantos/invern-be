meta {
  name: Delete Cart
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/private/carts/{{cartId}}
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403.
}
