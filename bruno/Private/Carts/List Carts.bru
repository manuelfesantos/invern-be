meta {
  name: List Carts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/private/carts?page=1&pageSize=10
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403.
}
