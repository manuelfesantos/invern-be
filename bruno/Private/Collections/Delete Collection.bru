meta {
  name: Delete Collection
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/private/collections/{{collectionId}}
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403. Cascades to its products + images.
}
