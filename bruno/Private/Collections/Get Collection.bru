meta {
  name: Get Collection
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/private/collections/{{collectionId}}
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403.
}
