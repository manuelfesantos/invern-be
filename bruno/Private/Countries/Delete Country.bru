meta {
  name: Delete Country
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/private/countries/{{countryCode}}
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403.
}
