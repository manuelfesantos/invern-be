meta {
  name: List Countries
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/private/countries
  body: none
  auth: none
}

docs {
  Requires an ADMIN session (run Public/Auth/Login (Admin) first). Anonymous -> 401, USER -> 403.
}
