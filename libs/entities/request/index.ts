export type Credentials = {
  userId?: string;
  cartId?: string;
  customerEmail?: string;
  accessToken?: string;
  refreshToken: string;
  remember?: boolean;
  address?: string;
  userDetails?: string;
  shippingMethod?: string;
};
