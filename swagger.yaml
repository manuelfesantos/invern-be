openapi: 3.0.0
info:
  title: Invern Spirit Backend API
  version: 1.0.0
  description: |
    API documentation for the Invern Spirit ecommerce backend, built with Cloudflare Workers.
    Handles product catalog, user management, shopping cart, checkout, and order processing.

servers:
  - url: http://localhost:8790 # `npm start` (wrangler dev --local --port 8790)
    description: Local Development Environment
  - url: https://api.invernspirit.com
    description: Production Environment
  - url: https://{preview-subdomain}.workers.dev
    description: >
      Preview Environment (Cloudflare Workers preview URL). The app runs as a
      Worker (not a Pages Function); each deploy gets a versioned
      `*.workers.dev` subdomain. Replace `{preview-subdomain}` with the one
      wrangler prints. (Repo migrating to the `invern-spirit` monorepo.)
    variables:
      preview-subdomain:
        default: preview-invern-be
        description: The versioned Workers preview subdomain from `wrangler deploy`.

tags:
  - name: Health
    description: Unauthenticated readiness probe for uptime monitoring.
  # Public
  - name: Public - Countries
    description: Operations related to country listing and selection.
  - name: Public - Images
    description: Public serving of uploaded image objects from R2 by key.
  - name: Public - Config
    description: Endpoint to fetch initial configuration (cart, user, tokens).
  - name: Public - Products
    description: Operations related to viewing products.
  - name: Public - Collections
    description: Operations related to viewing product collections.
  - name: Public - Cart
    description: Shopping cart management (anonymous and authenticated users).
  - name: Public - Checkout
    description: Multi-stage checkout process (requires authentication).
  - name: Public - User Auth
    description: User authentication operations (signup, login, logout, forgot password).
  - name: Public - User Profile
    description: User profile management operations (requires authentication).
  - name: Public - Orders
    description: Viewing user orders (requires authentication or guest email).
  - name: Public - OAuth
    description: OAuth related operations (Google).
  # Private (Admin/Internal)
  - name: Private - Currencies
    description: Admin operations for managing currencies. Requires Admin Authentication.
  - name: Private - Countries
    description: Admin operations for managing countries. Requires Admin Authentication.
  - name: Private - Products Admin
    description: Admin operations for managing products. Requires Admin Authentication.
  - name: Private - Collections Admin
    description: Admin operations for managing collections. Requires Admin Authentication.
  - name: Private - Carts Admin # Added
    description: Admin operations for viewing carts. Requires Admin Authentication.
  - name: Private - Users Admin # Added
    description: Admin operations for managing users. Requires Admin Authentication.
  - name: Private - Orders Admin
    description: Admin operations for viewing and managing all orders. Requires Admin Authentication. # Updated Description
  - name: Private - Stock
    description: Internal operations for managing product stock. Requires special auth/intended for specific triggers.
  - name: Private - Taxes
    description: Admin operations for managing per-country tax rates (mirrored to Stripe TaxRates). Requires Admin Authentication.
  - name: Private - Dashboard
    description: Admin operational summary (counts, low-stock, recent orders) for the backoffice home. Requires Admin Authentication.
  - name: Private - Images
    description: Admin operations for uploading image files to R2 and managing image records (product/collection association).
  - name: Private - Test Data
    description: Internal operations for seeding test data. Requires special auth.
  - name: Private - Scheduled Tasks
    description: Endpoints triggered by Cloudflare Cron Triggers or similar mechanisms.
  # Webhooks
  - name: Stripe Webhooks
    description: Webhooks for receiving events from Stripe.

security:
  - AdminBearer: [] # Define a scheme for private endpoints needing a secret key

paths:
  # -------------------- Health --------------------
  /health:
    get:
      tags:
        - Health
      summary: Readiness Probe
      description: >-
        Pings D1, KV and R2 and reports each dependency's status. Returns 200
        when all are reachable, 503 when any is down. Unauthenticated.
      responses:
        '200':
          description: All dependencies reachable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
        '503':
          description: One or more dependencies are unreachable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'

  # -------------------- Public - Countries --------------------
  /public/countries:
    get:
      tags:
        - Public - Countries
      summary: Get All Available Countries
      description: Retrieves a list of all countries supported by the store.
      responses:
        '200':
          description: A list of countries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting countries
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ClientCountry'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}:
    get:
      tags:
        - Public - Countries
      summary: Get Country Details by Code
      description: Retrieves detailed information for a specific country using its ISO 2-letter code (uppercase). Caches results.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
      responses:
        '200':
          description: Detailed information about the country.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Success getting country by code
                  data:
                    $ref: '#/components/schemas/Country' # Full country details
        '400':
          $ref: '#/components/responses/BadRequest' # e.g., Invalid country code format
        '404':
          $ref: '#/components/responses/NotFound' # Country not supported/found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Images --------------------
  /public/images/{key}:
    parameters:
      - name: key
        in: path
        required: true
        description: The R2 object key (the `<uuid>.<ext>` filename of the hosted image).
        schema:
          type: string
    get:
      tags:
        - Public - Images
      summary: Serve Image Object
      description: >
        Streams an uploaded image object from R2 by key. In production images are
        served directly from the R2 public domain (`IMAGES_HOST`); this route makes
        the upload URL resolvable on the worker itself (used locally, where the
        public domain isn't reachable). Public — no auth.
      responses:
        '200':
          description: The image bytes.
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound' # No object with that key
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Config --------------------
  /public/countries/{countryCode}/config:
    get:
      tags:
        - Public - Config
      summary: Get Initial API Configuration
      description: |
        Fetches initial data needed for the frontend, including anonymous/logged-in tokens,
        the current shopping cart state (from cookie `c_i` if present), and user details if logged in (validated via `s_r` refresh token cookie).
        Handles refresh token validation and issues new access tokens.
        Cookies (`s_r`, `c_i`, `r_m`, checkout stage cookies) determine the response state.
        The `after-checkout` header influences cookie handling after a successful payment redirect. Sets auth cookies if needed.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
        - name: after-checkout
          in: header
          required: false
          description: Set to 'true' after a successful checkout redirect to clean up session cookies (`c_s`, `a_d`, `u_d`, `s_m`) and potentially set `c_m`.
          schema:
            type: string
            enum: ['true']
      responses:
        '200':
          description: Configuration object containing tokens, cart, and potentially user data. Cookies may be set/deleted.
          content:
            application/json:
              schema:
                oneOf: # Response varies based on auth state
                  - $ref: '#/components/schemas/ConfigResponseLoggedIn'
                  - $ref: '#/components/schemas/ConfigResponseLoggedOut'
          headers:
            Set-Cookie:
              description: May set `s_r` (refresh token), `c_i` (cart ID), `r_m` (remember me), `c_m` (customer email), and delete checkout stage cookies (`a_d`, `u_d`, `s_m`, `c_s`).
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized' # If refresh token validation fails severely
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Products --------------------
  /public/countries/{countryCode}/products:
    get:
      tags:
        - Public - Products
      summary: Get All Products
      description: Retrieves a list of all available products for the specified country, optionally filtered by a search term. Prices include calculated taxes based on the country.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
        - $ref: '#/components/parameters/SearchQuery'
      responses:
        '200':
          description: A list of products available in the specified country.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Successfully got products
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ExtendedProduct'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/products/{id}:
    get:
      tags:
        - Public - Products
      summary: Get Product Details
      description: Retrieves detailed information for a specific product by its ID, including calculated taxes and collection details.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
        - $ref: '#/components/parameters/ProductId'
      responses:
        '200':
          description: Detailed information about the product.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Successfully got product
                  data:
                    $ref: '#/components/schemas/ExtendedProductWithCollectionDetails'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '404':
          $ref: '#/components/responses/NotFound' # Product or Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Collections --------------------
  /public/countries/{countryCode}/collections:
    get:
      tags:
        - Public - Collections
      summary: Get All Collections
      description: Retrieves a list of all product collections for the specified country.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
      responses:
        '200':
          description: A list of product collections.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting collections
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Collection'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/collections/{id}:
    get:
      tags:
        - Public - Collections
      summary: Get Collection Details
      description: Retrieves detailed information for a specific collection by its ID, including its products with calculated taxes.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
        - $ref: '#/components/parameters/CollectionId'
      responses:
        '200':
          description: Detailed information about the collection and its products.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting collection details
                  data:
                    $ref: '#/components/schemas/ExtendedCollectionDetails'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '404':
          $ref: '#/components/responses/NotFound' # Collection or Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Cart --------------------
  /public/countries/{countryCode}/cart:
    get:
      tags:
        - Public - Cart
      summary: Get Current Cart
      description: Retrieves the current user's shopping cart (anonymous or logged-in), identified by `c_i` cookie or user session. Includes product details, quantities, calculated prices, taxes, and totals. Issues new anonymous tokens/cart ID if none exist.
      parameters:
        - $ref: '#/components/parameters/CountryCode'
      responses:
        '200':
          description: The user's current shopping cart. May set `s_r` and `c_i` cookies if a new anonymous session is created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartResponse' # Includes ExtendedCart and accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional' # May set s_r, c_i
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/cart/items/{id}:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
      - $ref: '#/components/parameters/ProductId'
    put:
      tags:
        - Public - Cart
      summary: Set Cart Item Quantity (Put)
      description: Sets the exact quantity for a product in the cart. Adds the item if not present (requires quantity > 0). Removes the item if quantity is 0. Creates an anonymous cart (`c_i` cookie) if one doesn't exist. Updates `lastModifiedAt` on the cart. Deletes checkout stage cookies.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CartItemQuantityInputPut' # Minimum 0
      responses:
        '200':
          description: The updated shopping cart. May set `s_r` and `c_i` cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional' # Sets checkout stage cookies to empty, may set s_r, c_i
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid quantity (negative), product out of stock, invalid product ID
        '404':
          $ref: '#/components/responses/NotFound' # Product or Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags:
        - Public - Cart
      summary: Remove Item From Cart
      description: Removes a specific product entirely from the shopping cart. Requires cart to exist (identified by `c_i` or user session). Updates `lastModifiedAt` on the cart. Deletes checkout stage cookies.
      responses:
        '200':
          description: The updated shopping cart after removal. May set `s_r` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CartResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional' # Sets checkout stage cookies to empty, may set s_r
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid product ID, Cart does not exist
        '404':
          $ref: '#/components/responses/NotFound' # Product or Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Checkout --------------------
  /public/countries/{countryCode}/checkout/stages:
    get:
      tags:
        - Public - Checkout
      summary: Get Available Checkout Stages
      description: Retrieves the list of checkout stages and their current enabled status based on the cart and previously completed steps (stored in cookies `u_d`, `a_d`, `s_m`). Requires authentication (access token).
      parameters:
        - $ref: '#/components/parameters/CountryCode'
      responses:
        '200':
          description: List of available checkout stages and their status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStagesResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/BadRequest' # Cart is empty or has issues preventing checkout
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/checkout/stages/personal-details:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Checkout
      summary: Get Personal Details for Checkout
      description: Retrieves the personal details (email, first/last name) either from cookie `u_d` (guest) or the logged-in user profile. Requires authentication. Fails if this stage is not enabled.
      responses:
        '200':
          description: Personal details for checkout and current stage status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponsePersonalDetailsGet'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabled'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      tags:
        - Public - Checkout
      summary: Save Personal Details for Checkout (Guest Only)
      description: Saves personal details (email, first/last name) for a guest checkout. Only applicable if user is *not* logged in. Sets the `u_d` cookie (encrypted) and enables the next stage. Requires authentication (anonymous token is sufficient). Fails if this stage is not enabled or user is logged in.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckoutStageInputPersonalDetails'
      responses:
        '200':
          description: Personal details saved, returns updated stage status. Sets `u_d` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponsePersonalDetailsPost'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookiePersonalDetails'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Forbidden - Cannot set guest details when logged in.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/checkout/stages/address:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Checkout
      summary: Get Address for Checkout
      description: Retrieves the shipping address from cookie `a_d` (guest) or the logged-in user's saved profile address. Requires authentication. Fails if this stage is not enabled.
      responses:
        '200':
          description: Address details and stage status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponseAddressGet'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabled'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      tags:
        - Public - Checkout
      summary: Save Address for Checkout
      description: Saves the shipping address. Sets the `a_d` cookie (encrypted). Optionally saves to the user profile if logged in and `saveAddress` is true. Enables the next stage. Requires authentication. Fails if this stage is not enabled or address country doesn't match path country.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckoutStageInputAddress'
      responses:
        '200':
          description: Address saved, returns updated stage status. Sets `a_d` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponseAddressPost'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAddress'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest' # Also for country mismatch
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/checkout/stages/shipping:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Checkout
      summary: Get Available Shipping Methods
      description: Retrieves available shipping methods based on cart weight and destination country. Returns the currently selected method if `s_m` cookie is set. Requires authentication. Fails if this stage is not enabled.
      responses:
        '200':
          description: List of shipping methods and stage status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponseShippingGet'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest' # Also for cart invalid
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # No suitable shipping methods/rates found or country not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      tags:
        - Public - Checkout
      summary: Select Shipping Method
      description: Selects a shipping method/rate by its ID (must be a valid rate for the current cart/country). Sets the `s_m` cookie (encrypted shipping *method* ID). Enables the next stage. Requires authentication. Fails if this stage is not enabled or rate is invalid/not applicable.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckoutStageInputShipping' # ID of the shipping *method*
      responses:
        '200':
          description: Shipping method selected, returns updated stage status. Sets `s_m` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponseShippingPost'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieShipping'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest' # Also for invalid input/rate not applicable
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Shipping method/rate not found or country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/checkout/stages/review:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Checkout
      summary: Get Checkout Review
      description: Retrieves a summary of the order (cart items, personal details, address, selected shipping) before proceeding to payment. Requires authentication. Fails if this stage is not enabled or preceding stages are incomplete.
      responses:
        '200':
          description: Order review details.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponseReviewGet'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest' # Also for preceding stages incomplete
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/checkout/stages/payment:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Checkout
      summary: Get Payment Session URL
      description: Creates a Stripe Checkout session and returns the URL to redirect the user for payment. Reserves stock. Sets the `c_s` cookie (encrypted checkout session ID). Requires authentication and all previous checkout stages to be complete. Fails if the review stage is not enabled or cart has issues (e.g., stock).
      responses:
        '200':
          description: Stripe Checkout session URL. Sets `c_s` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckoutStageResponsePaymentGet'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieCheckoutSession'
        '400':
          $ref: '#/components/responses/CheckoutStageNotEnabledOrBadRequest' # Also for preceding stages incomplete/invalid cart state/stock issues
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Country not found
        '500':
          $ref: '#/components/responses/InternalServerError' # Stripe API error, stock reservation issues

  # -------------------- Public - User Auth --------------------
  /public/countries/{countryCode}/user/signup:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: User Signup
      description: Creates a new, initially unvalidated user account. Sends a validation email. Fails if user is already logged in or email is taken by a validated user. If email exists but is unvalidated/OAuth, updates the record. Associates anonymous cart (`c_i` cookie) if present. Returns success message, requires email validation to complete login.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignupInput'
      responses:
        '200':
          description: Signup initiated, validation email sent. Returns anonymous tokens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignupResponse' # Contains message and anonymous accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAnonymous' # Sets anonymous s_r, deletes c_i and checkout cookies
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized' # User already logged in
        '409':
          $ref: '#/components/responses/Conflict' # Email already taken by a validated, non-OAuth user
        '500':
          $ref: '#/components/responses/InternalServerError' # Email sending or DB error

  /public/countries/{countryCode}/user/signup/verify-email:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: Verify Signup Email
      description: Validates the code sent during signup. If valid, marks the user as validated, logs them in, and returns user data, cart, and auth tokens/cookies. Deletes validation secret from KV.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyEmailInput'
      responses:
        '200':
          description: Email verified, user logged in. Returns user data, cart, and tokens. Sets auth cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSuccessResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAuth' # Sets s_r, r_m, deletes c_i, checkout cookies
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input
        '401':
          description: Invalid or expired code, or too many attempts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # User or code not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/signup/resend-email:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: Resend Signup Verification Email
      description: Resends the verification email if the user exists, is unvalidated, and hasn't exceeded the resend limit. Uses the existing code stored in KV.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResendEmailInput'
      responses:
        '200':
          description: Verification email resent successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse' # Contains message and anonymous accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAnonymous' # May refresh anonymous s_r
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input
        '401':
          description: Too many resend attempts or code expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # User or original code not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/login:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: User Login
      description: Logs in an existing, validated user with email and password. Returns user data, cart, and new auth tokens/cookies. Merges anonymous cart (`c_i` cookie) if present.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginInput'
      responses:
        '200':
          description: User successfully logged in. Returns user data, cart, and tokens. Sets auth cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSuccessResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAuth' # Sets s_r, r_m, deletes c_i, checkout cookies
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          description: Invalid credentials or user not validated/already logged in.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/logout:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: User Logout
      description: Logs out the current user. Invalidates the refresh token server-side (by deleting from KV). Returns anonymous tokens and an empty cart representation. Requires authentication. Deletes auth cookies.
      responses:
        '200':
          description: User successfully logged out. Returns anonymous tokens and empty cart. Deletes auth cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogoutResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieLogout'
        '401':
          $ref: '#/components/responses/Unauthorized' # User not logged in
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/forgot-password/submit-email:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: Submit Forgot Password Email
      description: Initiates the password reset process. Sends an email with a verification code to the user if the email exists and is validated. Creates/updates KV entry with code, expiry, attempts.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitForgotPasswordEmailInput'
      responses:
        '200':
          description: Email sent successfully (or process initiated; avoids user enumeration). Returns anonymous tokens.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Email sent successfully
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAnonymous'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid email format
        '401':
          description: Too many requests (code exhausted) for this email recently.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found or not validated (Response is 200 to prevent enumeration, but internally no email sent).
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Email sent successfully
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/forgot-password/validate-code:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: Validate Forgot Password Code
      description: Validates the verification code sent via email against the entry in KV. Decrements attempts on failure.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateCodeInput'
      responses:
        '200':
          description: Code validated successfully. Returns anonymous tokens.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Code validated successfully
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAnonymous'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input
        '401':
          description: Invalid or expired code, or too many attempts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # Code not found (likely expired and deleted or never existed)
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/forgot-password/reset:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Auth
      summary: Reset Password
      description: Resets the user's password using a validated code. Requires email, code, and new password. Validates code against KV again, then updates user password hash and deletes KV entry. Invalidates existing refresh token by creating a new one.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordInput'
      responses:
        '200':
          description: Password reset successfully. Returns anonymous tokens.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Password reset successfully
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAnonymous'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input
        '401':
          description: Invalid or expired code, or too many attempts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # User or code not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - User Profile --------------------
  /public/countries/{countryCode}/user:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - User Profile
      summary: Get User Profile
      description: Retrieves the profile information (email, name, address) for the currently authenticated user. Requires authentication.
      responses:
        '200':
          description: User profile data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUserResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # User record somehow missing
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags:
        - Public - User Profile
      summary: Delete User Account
      description: Permanently deletes the authenticated user's account and associated data (cart, orders might be kept or anonymized based on policy). Invalidates tokens. Requires authentication. Returns anonymous tokens.
      responses:
        '200':
          description: User account successfully deleted. Returns anonymous tokens. Deletes auth cookies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteUserResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieLogout'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/update/personal-information:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Profile
      summary: Update User Personal Information
      description: Updates the authenticated user's first name, last name, and/or address. Requires authentication. Address country must match path country.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePersonalInformationInput'
      responses:
        '200':
          description: Personal information successfully updated. Returns updated user profile data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateUserResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data or address country mismatch
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # User not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/update/password:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Profile
      summary: Update User Password
      description: Updates the authenticated user's password. Requires current password for verification. Requires authentication. Invalidates existing refresh token by creating a new one.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordInput'
      responses:
        '200':
          description: Password successfully updated. Returns updated user profile data and new tokens.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateUserResponse' # Contains user DTO and new accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAuth' # Sets new s_r
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized' # Incorrect current password or not logged in
        '404':
          $ref: '#/components/responses/NotFound' # User not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/user/update/email/submit:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Profile
      summary: Submit Email Change Request
      description: Initiates the process to change the authenticated user's email. Sends a verification code to the *new* email address. Requires authentication. Stores the new email and code temporarily (e.g., in KV).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitEmailChangeInput'
      responses:
        '200':
          description: Verification email sent to the new address. Returns current user profile data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUserResponse' # Contains current user DTO and accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input email format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict' # New email address is already taken
        '500':
          $ref: '#/components/responses/InternalServerError' # KV or email sending error

  /public/countries/{countryCode}/user/update/email/validate-code:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    post:
      tags:
        - Public - User Profile
      summary: Validate Email Change Code
      description: Validates the code sent to the new email address. If valid, updates the user's email in the database and deletes the temporary validation secret. Requires authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateEmailChangeCodeInput'
      responses:
        '200':
          description: Email successfully updated. Returns updated user profile data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateUserResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input
        '401':
          description: Invalid or expired code, or too many attempts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/NotFound' # User or code not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - Orders --------------------
  /public/countries/{countryCode}/orders:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - Orders
      summary: Get User Orders
      description: Retrieves a list of past orders for the authenticated user. Requires authentication.
      responses:
        '200':
          description: A list of the user's orders.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetUserOrdersResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptional'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # User has no orders, or user not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/orders/{id}:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
      - $ref: '#/components/parameters/OrderId'
      - name: x-user-email # Optional header for guest access
        in: header
        required: false
        description: Email address for guest order lookup (alternative to cookie `c_m`). Used only if user is not authenticated via JWT.
        schema:
          type: string
          format: email
    get:
      tags:
        - Public - Orders
      summary: Get Order Details
      description: Retrieves detailed information for a specific order by its ID. Requires authentication (either logged-in user matching order OR guest providing matching email via `x-user-email` header or `c_m` cookie). Sets `c_m` cookie if email header was used successfully.
      responses:
        '200':
          description: Detailed information about the order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOptionalOrCustomerEmail'
        '401':
          $ref: '#/components/responses/Unauthorized' # User doesn't own order / Email doesn't match or not provided for guest
        '404':
          $ref: '#/components/responses/NotFound' # Order not found
        '422':
          description: Unprocessable Entity - Email required for guest lookup but not provided via header or cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Public - OAuth --------------------
  /public/countries/{countryCode}/oauth/google/redirect:
    parameters:
      - $ref: '#/components/parameters/CountryCode'
    get:
      tags:
        - Public - OAuth
      summary: Get Google OAuth Redirect URL
      description: Generates the URL to redirect the user to Google for authentication. Sets an `o_t` cookie (HttpOnly, Secure, SameSite=Lax) for state validation.
      responses:
        '200':
          description: Returns the Google OAuth URL. Sets `o_t` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthRedirectResponse'
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieOAuthToken'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /public/countries/{countryCode}/oauth/google/callback:
    parameters:
      - $ref: '#/components/parameters/CountryCode' # Note: Country code in path might be ignored if state parameter overrides it
      - name: code
        in: query
        required: true
        description: Authorization code from Google.
        schema:
          type: string
      - name: state
        in: query
        required: true
        description: State parameter containing original country and OAuth token, JSON stringified.
        schema:
          type: string
          example: '{"country":"pt","oauthToken":"..."}'
    get:
      tags:
        - Public - OAuth
      summary: Handle Google OAuth Callback
      description: Handles the callback from Google after successful authentication. Validates state against `o_t` cookie, exchanges code for tokens, retrieves user info, logs in or signs up the user, and returns auth tokens/cookies.
      responses:
        '200':
          description: User successfully authenticated via Google. Returns tokens. Sets auth cookies. Deletes `o_t` cookie.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthCallbackResponse' # Contains accessToken
          headers:
            Set-Cookie:
              $ref: '#/components/headers/SetCookieAuthAndOAuthDelete' # Sets s_r and r_m, deletes o_t
        '400':
          $ref: '#/components/responses/BadRequest' # Missing code or state, or invalid state JSON
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid state/token mismatch from cookie
        '404':
          $ref: '#/components/responses/NotFound' # Google user info not found after successful code exchange
        '500':
          $ref: '#/components/responses/InternalServerError' # Error communicating with Google or DB issues

  # -------------------- Private - Currencies --------------------
  /private/currencies:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Currencies
      summary: Get All Currencies (Admin)
      description: >-
        Retrieves a paginated list of currencies. Supports sorting and
        filtering; the response `total` reflects any applied filter. Requires
        Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [name, code, rateToEuro, createdAt]
        - name: name
          in: query
          required: false
          description: Filter to currencies whose name contains this term.
          schema:
            type: string
        - name: code
          in: query
          required: false
          description: Filter to currencies whose code contains this term.
          schema:
            type: string
      responses:
        '200':
          description: A list of currencies.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/Currency' # Full currency details including rateToEuro
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Currencies
      summary: Add New Currency (Admin)
      description: Adds a new currency to the system. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCurrency'
      responses:
        '200': # Using 200 as per existing patterns, could be 201
          description: Currency successfully created. Returns the created currency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Currency'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '409':
          $ref: '#/components/responses/Conflict' # Currency code already exists
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/currencies/{code}:
    parameters:
      - name: code
        in: path
        required: true
        description: ISO 3-letter currency code (e.g., EUR, USD).
        schema:
          type: string
          example: EUR
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Currencies
      summary: Get Currency by Code (Admin)
      description: Retrieves details for a specific currency. Requires Admin Authentication.
      responses:
        '200':
          description: Currency details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Currency'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid code format
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '404':
          $ref: '#/components/responses/NotFound' # Currency not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Currencies
      summary: Update Currency (Admin)
      description: Updates details for an existing currency. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCurrency' # Use Insert schema for update payload
      responses:
        '200':
          description: Currency successfully updated. Returns the updated currency.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Currency'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '404':
          $ref: '#/components/responses/NotFound' # Currency not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Currencies
      summary: Delete Currency (Admin)
      description: Deletes a currency from the system. Requires Admin Authentication. Be cautious as this might break associations.
      responses:
        '200':
          description: Currency successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid code format
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '404':
          $ref: '#/components/responses/NotFound' # Currency not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Countries --------------------
  /private/countries:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Countries
      summary: Get All Countries (Admin)
      description: >-
        Retrieves a paginated list of countries. Supports sorting and
        filtering; the response `total` reflects any applied filter. Requires
        Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [name, code, createdAt]
        - name: name
          in: query
          required: false
          description: Filter to countries whose name contains this term.
          schema:
            type: string
        - name: code
          in: query
          required: false
          description: Filter to countries whose code contains this term.
          schema:
            type: string
        - name: currencyCode
          in: query
          required: false
          description: Filter to countries using this currency (exact match).
          schema:
            type: string
      responses:
        '200':
          description: A list of countries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/ClientCountry' # Use ClientCountry for listing
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Countries
      summary: Add New Country (Admin)
      description: Adds a new country to the system. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCountry'
      responses:
        '200': # Using 200 as per existing patterns
          description: Country successfully created. Returns the created country details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Country' # Return full details on create
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict' # Country code already exists
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/countries/{code}:
    parameters:
      - name: code
        in: path
        required: true
        description: ISO 2-letter country code (e.g., PT, ES).
        schema:
          type: string
          example: PT
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Countries
      summary: Get Country by Code (Admin)
      description: Retrieves details for a specific country. Requires Admin Authentication.
      responses:
        '200':
          description: Country details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Country'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Countries
      summary: Update Country (Admin)
      description: Updates details for an existing country. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCountry' # Use Insert schema for update payload
      responses:
        '200':
          description: Country successfully updated. Returns the updated country details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/Country'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Countries
      summary: Delete Country (Admin)
      description: Deletes a country from the system. Requires Admin Authentication. Be cautious as this might break associations (taxes, shipping rates).
      responses:
        '200':
          description: Country successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Products Admin --------------------
  /private/products:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Products Admin
      summary: Get All Products (Admin)
      description: >-
        Retrieves a paginated list of products without extended pricing.
        Supports sorting and filtering; the response `total` reflects any
        applied filter. Requires Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [name, stock, priceInCents, createdAt]
        - name: collectionId
          in: query
          required: false
          description: Filter to products in this collection (exact match).
          schema:
            type: string
        - name: name
          in: query
          required: false
          description: Filter to products whose name contains this term.
          schema:
            type: string
        - name: maxStock
          in: query
          required: false
          description: Filter to products with stock at or below this value (low-stock).
          schema:
            type: integer
            minimum: 0
      responses:
        '200':
          description: A list of products.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/Product' # Basic product info
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Products Admin
      summary: Add New Product (Admin)
      description: Adds a new product to the system. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertProduct'
      responses:
        '200': # Using 200 as per existing patterns
          description: Product successfully created. Returns the ID of the created product.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      productId:
                        type: string
                        format: uuid
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized' # Invalid admin auth
        '404':
          $ref: '#/components/responses/NotFound' # e.g., Referenced collection not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/products/{id}:
    parameters:
      - $ref: '#/components/parameters/ProductId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Products Admin
      summary: Get Product Details (Admin)
      description: Retrieves detailed information for a specific product by its ID. Requires Admin Authentication.
      responses:
        '200':
          description: Detailed information about the product.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Product fetched successfully
                  data:
                    $ref: '#/components/schemas/ProductWithCollectionDetails' # includes collection { id, name }
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Product not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Products Admin
      summary: Update Product (Admin)
      description: Updates details for an existing product. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProduct' # stock is not editable via product update
      responses:
        '200':
          description: Product successfully updated. Returns the updated product details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ProductDetails'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Product or related collection not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Products Admin
      summary: Delete Product (Admin)
      description: Deletes a product from the system. Requires Admin Authentication.
      responses:
        '200':
          description: Product successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Product not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Collections Admin --------------------
  /private/collections:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Collections Admin
      summary: Get All Collections (Admin)
      description: >-
        Retrieves a paginated list of collections. Supports sorting and
        filtering; the response `total` reflects any applied filter. Requires
        Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [name, createdAt]
        - name: name
          in: query
          required: false
          description: Filter to collections whose name contains this term.
          schema:
            type: string
      responses:
        '200':
          description: A list of collections.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/Collection' # Basic collection info
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Collections Admin
      summary: Add New Collection (Admin)
      description: Adds a new collection to the system. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCollection'
      responses:
        '200': # Using 200 as per existing patterns
          description: Collection successfully created. Returns the created collection details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/CollectionDetails'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/collections/{id}:
    parameters:
      - $ref: '#/components/parameters/CollectionId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Collections Admin
      summary: Get Collection Details (Admin)
      description: Retrieves detailed information for a specific collection by its ID, including its products. Requires Admin Authentication.
      responses:
        '200':
          description: Detailed information about the collection and its products.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/CollectionDetails' # Admin needs full details
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Collection not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Collections Admin
      summary: Update Collection (Admin)
      description: Updates details for an existing collection. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertCollection' # Use Insert schema for update payload
      responses:
        '200':
          description: Collection successfully updated. Returns the updated collection details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/CollectionDetails'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Collection not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Collections Admin
      summary: Delete Collection (Admin)
      description: Deletes a collection from the system. Requires Admin Authentication.
      responses:
        '200':
          description: Collection successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Collection not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Carts Admin (NEW) --------------------
  /private/carts:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Carts Admin
      summary: Get All Carts (Admin)
      description: >-
        Retrieves a paginated list of carts. Supports sorting and filtering;
        the response `total` reflects any applied filter. Requires Admin
        Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [createdAt, lastModifiedAt]
        - name: isLoggedIn
          in: query
          required: false
          description: Filter carts by logged-in state.
          schema:
            type: boolean
      responses:
        '200':
          description: A list of carts.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Carts fetched successfully
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/Cart' # Full Cart schema
                        required:
                          - data
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid pagination parameters
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/carts/{id}:
    parameters:
      - $ref: '#/components/parameters/CartId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Carts Admin
      summary: Get Cart by ID (Admin)
      description: Retrieves detailed information for a specific cart by its ID. Requires Admin Authentication.
      responses:
        '200':
          description: Detailed information about the cart.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Cart fetched successfully
                  data:
                    $ref: '#/components/schemas/Cart' # Full Cart schema
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Cart not found
        '500':
          $ref: '#/components/responses/InternalServerError'

    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Carts Admin
      summary: Delete Cart by ID (Admin)
      description: Deletes a cart by its ID. Requires Admin Authentication.
      responses:
        '200':
          description: Cart successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Cart not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Users Admin (NEW) --------------------
  /private/users:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Users Admin
      summary: Get All Users (Admin)
      description: >-
        Retrieves a paginated list of users. Supports sorting and filtering;
        the response `total` reflects any applied filter. Requires Admin
        Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [createdAt, email]
        - name: email
          in: query
          required: false
          description: Filter to users whose email contains this term.
          schema:
            type: string
        - name: role
          in: query
          required: false
          description: Filter users by role (exact match).
          schema:
            type: string
            enum: [ADMIN, USER]
        - name: isValidated
          in: query
          required: false
          description: Filter users by email-validation state.
          schema:
            type: boolean
      responses:
        '200':
          description: A list of users.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Users fetched successfully
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/AdminUser' # Safe projection (no password/googleId)
                        required:
                          - data
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid pagination parameters
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/users/{id}:
    parameters:
      - $ref: '#/components/parameters/UserId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Users Admin
      summary: Get User by ID (Admin)
      description: Retrieves detailed information for a specific user by their ID. Requires Admin Authentication.
      responses:
        '200':
          description: Detailed user information.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: User fetched successfully
                  data:
                    $ref: '#/components/schemas/AdminUser'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # User not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Users Admin
      summary: Update User (Admin)
      description: >-
        Updates a user's admin-managed state — `role`, `isValidated`, and
        `disabled` (whitelisted; other fields ignored). A role change or a
        disable revokes the target's session. The last remaining admin cannot be
        demoted or disabled (409). Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserInput'
      responses:
        '200':
          description: Updated user (safe projection).
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/AdminUser'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Would remove the last admin (demote or disable).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Users Admin
      summary: Delete User by ID (Admin)
      description: >-
        Hard-deletes a user account by its ID (guarded: the last admin cannot be
        deleted → 409). Prefer disabling for problem accounts. Requires Admin
        Authentication.
      responses:
        '200':
          description: User successfully deleted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # User not found
        '409':
          description: Would delete the last admin.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Orders Admin --------------------
  /private/orders:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Orders Admin
      summary: Get All Orders (Admin)
      description: >-
        Retrieves a paginated list of orders. Supports sorting and filtering
        (the exact-match id filters combine with AND); the response `total`
        reflects any applied filter. Requires Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [createdAt, lastModifiedAt, isCanceled]
        - name: userId
          in: query
          required: false
          description: Filter orders by User ID.
          schema:
            type: string
            format: uuid
        - name: paymentId
          in: query
          required: false
          description: Filter orders by Payment ID.
          schema:
            type: string
        - name: stripeId
          in: query
          required: false
          description: Filter orders by Stripe Checkout Session ID.
          schema:
            type: string
        - name: shippingTransactionId
          in: query
          required: false
          description: Filter orders by Shipping Transaction ID.
          schema:
            type: string
            format: uuid
        - name: isCanceled
          in: query
          required: false
          description: Filter orders by cancellation state.
          schema:
            type: boolean
      responses:
        '200':
          description: A list of all orders matching the filters.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/Order' # Use full Order schema for admin
                        required:
                          - data
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid query parameters
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/orders/{id}:
    parameters:
      - $ref: '#/components/parameters/OrderId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Orders Admin
      summary: Get Order Details (Admin)
      description: Retrieves detailed information for a specific order by its ID. Requires Admin Authentication. Returns the full Order model including internal IDs.
      responses:
        '200':
          description: Detailed information about the order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Order fetched successfully
                  data:
                    $ref: '#/components/schemas/Order'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Order not found
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Orders Admin
      summary: Update Order (Admin)
      description: Updates details for an existing order. Requires Admin Authentication. Use with caution.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAdminOrderInput' # Define this schema
      responses:
        '200':
          description: Order successfully updated. Returns the updated order details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Order updated successfully
                  data:
                    $ref: '#/components/schemas/Order'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid input data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Order not found
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/orders/{id}/cancel:
    parameters:
      - $ref: '#/components/parameters/OrderId'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Orders Admin
      summary: Cancel Order (Admin)
      description: Marks an order as cancelled. Requires Admin Authentication. This might trigger stock release depending on implementation.
      responses:
        '200':
          description: Order successfully cancelled. Returns the updated order details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Order cancelled successfully
                  data:
                    $ref: '#/components/schemas/Order' # Shows order with isCanceled=true
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid ID format
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # Order not found
        '500':
          $ref: '#/components/responses/InternalServerError' # E.g., failed to release stock

  /private/orders/{id}/fulfillment:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Orders Admin
      summary: Update Order Fulfillment (Admin)
      description: >-
        Sets the order's shipping-transaction `status` (validated against the
        transition state machine) and/or `trackingUrl`. Addressed by order id.
        Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FulfillmentInput'
      responses:
        '200':
          description: The order with its updated shipping transaction.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Order fulfillment updated successfully
                  data:
                    $ref: '#/components/schemas/Order'
        '400':
          description: Missing status/trackingUrl, or trackingUrl is not an https URL.
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Illegal fulfillment status transition.
        '401':
          $ref: '#/components/responses/Unauthorized'

  # -------------------- Private - Scheduled Tasks --------------------
  # Maintenance/cleanup routes mounted under /private/expired. Admin-gated; also
  # the endpoints a cron trigger calls to purge expired data.
  /private/expired/carts:
    delete:
      tags:
        - Private - Scheduled Tasks
      summary: Delete Expired Carts
      description: Purges anonymous carts that have passed their expiry window. Triggered on a cron schedule or manually by an admin.
      security:
        - AdminBearer: []
      responses:
        '200':
          description: Cleanup completed; message reports how many carts were deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Deleted 3 expired carts
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/expired/sessions:
    delete:
      tags:
        - Private - Scheduled Tasks
      summary: Delete Expired Checkout Sessions
      description: Finds and invalidates expired Stripe Checkout sessions and releases their reserved product stock. Triggered on a cron schedule or manually by an admin.
      security:
        - AdminBearer: []
      responses:
        '200':
          description: Cleanup completed; message indicates whether any sessions were expired.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: No sessions were expired
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/expired/users:
    delete:
      tags:
        - Private - Scheduled Tasks
      summary: Delete Expired Users
      description: Purges unverified/expired user records. Triggered on a cron schedule or manually by an admin.
      security:
        - AdminBearer: []
      responses:
        '200':
          description: Cleanup completed; message reports how many users were deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Deleted 2 expired users
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Shipping Admin --------------------
  /private/shipping/methods:
    get:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: List Shipping Methods (Admin)
      description: >-
        Paginated list of shipping methods, each with its rates. Supports sorting
        and filtering; the envelope `total` reflects any applied filter. Requires
        Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values -> 400).
          schema:
            type: string
            enum: [name, createdAt]
        - name: name
          in: query
          required: false
          description: Filter to methods whose name contains this term.
          schema:
            type: string
      responses:
        '200':
          description: A paginated list of shipping methods.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/ShippingMethod'
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Create Shipping Method (Admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertShippingMethod'
      responses:
        '200':
          description: The created method (with an empty rates array).
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingMethod'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /private/shipping/methods/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Get Shipping Method (Admin)
      description: Returns the method with all of its rates (unfiltered by weight).
      responses:
        '200':
          description: The shipping method with its rates.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingMethod'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Update Shipping Method (Admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertShippingMethod'
      responses:
        '200':
          description: The updated method (with its rates).
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingMethod'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    delete:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Delete Shipping Method (Admin)
      description: >-
        Deletes the method. Returns 409 if it still has rates (delete/reassign
        the rates first). Historical orders are unaffected (they store a
        serialized snapshot).
      responses:
        '200':
          description: Method deleted.
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The method still has rates; remove them first.
        '401':
          $ref: '#/components/responses/Unauthorized'

  /private/shipping/methods/{methodId}/rates:
    parameters:
      - name: methodId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: List Rates for a Method (Admin)
      description: All rates for the method as a plain list (bounded by weight bands; not paginated).
      responses:
        '200':
          description: The method's rates.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ShippingRate'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Create a Rate under a Method (Admin)
      description: >-
        Validates the weight band (`minWeight <= maxWeight`) and that it does not
        overlap the method's other rates. Bands are half-open `[min, max)`, so
        adjacent bands do not clash.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertShippingRate'
      responses:
        '200':
          description: The created rate.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingRate'
        '400':
          description: Invalid weight band (`minWeight > maxWeight`) or bad payload.
        '404':
          description: Parent shipping method not found.
        '409':
          description: The weight band overlaps an existing rate for this method.
        '401':
          $ref: '#/components/responses/Unauthorized'

  /private/shipping/methods/{methodId}/rates/{rateId}:
    parameters:
      - name: methodId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: rateId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Get a Rate (Admin)
      responses:
        '200':
          description: The rate, including its country codes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingRate'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Update a Rate (Admin)
      description: Re-validates the weight band and overlap (excluding the rate itself).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InsertShippingRate'
      responses:
        '200':
          description: The updated rate.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingRate'
        '400':
          description: Invalid weight band or bad payload.
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: The weight band overlaps another rate for this method.
        '401':
          $ref: '#/components/responses/Unauthorized'
    delete:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Delete a Rate (Admin)
      description: Deletes the rate and cascades its country assignments.
      responses:
        '200':
          description: Rate deleted.
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /private/shipping/methods/{methodId}/rates/{rateId}/countries:
    parameters:
      - name: methodId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: rateId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      security:
        - AdminBearer: []
      tags: [Private - Shipping Admin]
      summary: Set a Rate's Countries (Admin)
      description: >-
        Replaces the full set of countries the rate applies to, transactionally.
        Unknown country codes are rejected (400); duplicates are deduped. An
        empty set is allowed but leaves the rate unselectable at checkout.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RateCountriesInput'
      responses:
        '200':
          description: The rate with its resulting country set.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/ShippingRate'
        '400':
          description: Unknown or malformed country code(s).
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'

  # -------------------- Private - Stock --------------------
  /private/stock/setup:
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Stock
      summary: Initialize Stock Bucket
      description: Initializes the R2 stock bucket by putting stock levels for all existing products. Requires a secret key for authorization.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecretKeyInput'
      responses:
        '200':
          description: Stock bucket successfully initialized.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success setting up stock in bucket
        '401':
          $ref: '#/components/responses/Unauthorized' # Missing or incorrect secret key
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/stock/{productId}:
    parameters:
      - $ref: '#/components/parameters/ProductId'
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Stock
      summary: Get Stock (Local Dev Only)
      description: Retrieves stock information for a specific product directly from the R2 bucket. Intended for local development/debugging only. Returns 405 if accessed in non-local environment.
      responses:
        '200':
          description: Stock data for the product.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting stock
                  data:
                    type: integer
                    format: int32
                    description: Current stock quantity.
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid productId
        '404':
          $ref: '#/components/responses/NotFound' # Product stock not found in bucket
        '405':
          description: Method Not Allowed (if accessed in non-local env).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/images/upload:
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Images
      summary: Upload Image File
      description: Uploads an image file to the R2 images bucket under a collision-proof `<uuid>.<ext>` key and returns its hosted URL. Accepts `multipart/form-data` with a single `file` field. Only jpeg/png/webp/gif up to 5MB are accepted.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Image uploaded; returns its hosted URL.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Image uploaded successfully
                  data:
                    $ref: '#/components/schemas/ImageUploadResponseData'
        '400':
          $ref: '#/components/responses/BadRequest' # Missing file, disallowed content type, empty, or over size limit
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/images:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Images
      summary: List Product Images
      description: Returns all image records associated with a product.
      parameters:
        - name: productId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The product's image records.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Images fetched successfully
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Image'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Images
      summary: Create Image Record
      description: Registers an uploaded image URL as a database record and associates it with a product and/or collection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateImageInput'
      responses:
        '200':
          description: Image record created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Image created successfully
                  data:
                    $ref: '#/components/schemas/Image'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid body
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Images
      summary: Update Image Record
      description: Updates an image record's editable fields. The `url` in the body identifies the record (it is the primary key).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateImageInput'
      responses:
        '200':
          description: Image record updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Image updated successfully
                  data:
                    $ref: '#/components/schemas/Image'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid body
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # No image with that url
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/images/{key}:
    parameters:
      - name: key
        in: path
        required: true
        description: The R2 object key of the image (the `<uuid>.<ext>` filename, i.e. the path segment of its hosted URL).
        schema:
          type: string
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Images
      summary: Delete Image
      description: Deletes an image record and its underlying R2 object, so deleting an image never leaks storage.
      responses:
        '200':
          description: Image deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Image deleted successfully
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound' # No image with that url
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Session --------------------
  /private/me:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Users Admin
      summary: Get Current Admin (Admin)
      description: >-
        The signed-in admin's own profile (safe projection incl. role). The
        backoffice calls this after login to confirm ADMIN and show identity.
        Requires Admin Authentication.
      responses:
        '200':
          description: The current admin.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/AdminUser'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Dashboard --------------------
  /private/dashboard:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Dashboard
      summary: Get Dashboard Summary (Admin)
      description: >-
        A cheap operational overview for the backoffice home: entity counts,
        low-stock products, and the most recent orders. Computed in one D1 batch
        (COUNT(*)s + bounded selects). Requires Admin Authentication.
      responses:
        '200':
          description: The dashboard summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/DashboardSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Private - Taxes --------------------
  /private/taxes:
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Taxes
      summary: List Taxes (Admin)
      description: >-
        Paginated list of per-country tax rates. Filter by `countryCode`. `total`
        reflects the applied filter. Requires Admin Authentication.
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PageSizeParam'
        - $ref: '#/components/parameters/SortOrderParam'
        - name: sortBy
          in: query
          required: false
          description: Field to sort by (allow-listed; other values → 400).
          schema:
            type: string
            enum: [name, createdAt]
        - name: countryCode
          in: query
          required: false
          description: Filter to taxes for this exact country code.
          schema:
            type: string
            pattern: '^[A-Z]{2}$'
      responses:
        '200':
          description: A list of taxes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    allOf:
                      - $ref: '#/components/schemas/PaginationMeta'
                      - type: object
                        properties:
                          data:
                            type: array
                            items:
                              $ref: '#/components/schemas/AdminTax'
                        required:
                          - data
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      security:
        - AdminBearer: []
      tags:
        - Private - Taxes
      summary: Create Tax (Admin)
      description: >-
        Creates a Stripe TaxRate and stores a D1 row keyed by its id (so checkout
        can charge via it). `rate` is a fraction; Stripe receives the percentage.
        Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaxInput'
      responses:
        '200':
          description: Tax created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/AdminTax'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid body or unknown country
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /private/taxes/{id}:
    parameters:
      - name: id
        in: path
        required: true
        description: The tax id (the Stripe TaxRate id, `txr_…`).
        schema:
          type: string
    get:
      security:
        - AdminBearer: []
      tags:
        - Private - Taxes
      summary: Get Tax (Admin)
      responses:
        '200':
          description: The tax.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/AdminTax'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      security:
        - AdminBearer: []
      tags:
        - Private - Taxes
      summary: Update Tax (Admin)
      description: >-
        Updates name/description/active (mirrored to the Stripe TaxRate). The rate
        and country are immutable (Stripe percentages can't change) — sending
        either returns 409. Requires Admin Authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaxInput'
      responses:
        '200':
          description: Tax updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  data:
                    $ref: '#/components/schemas/AdminTax'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Attempted to change an immutable field (rate or countryCode).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      security:
        - AdminBearer: []
      tags:
        - Private - Taxes
      summary: Delete Tax (Admin)
      description: >-
        Archives the Stripe TaxRate (active:false — they can't be hard-deleted)
        and removes the D1 row. Requires Admin Authentication.
      responses:
        '200':
          description: Tax deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Tax deleted successfully
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # -------------------- Stripe Webhooks --------------------
  /stripe/session-result:
    post:
      tags:
        - Stripe Webhooks
      summary: Handle Stripe Checkout Session Events
      description: Webhook endpoint to receive `checkout.session.completed` and `checkout.session.expired` events from Stripe. Creates orders or handles expiration logic (releasing stock). Verifies Stripe signature (assumed handled by middleware or infrastructure). Ignores events not matching the configured `STRIPE_ENV`.
      requestBody:
        required: true
        description: Stripe Event object (`checkout.session.completed` or `checkout.session.expired`).
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StripeEvent' # Generic Stripe event, specific type checked internally
      responses:
        '200':
          description: Event received and processed successfully. Returns created order data for completed sessions, or success message for expired/ignored sessions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting checkout-session # Or 'checkout session already expired', 'Unsupported event...'
                  data: # Only present for newly created order on completed session
                    $ref: '#/components/schemas/ClientOrder'
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid payload or event type, or Stripe signature validation failure
        '409':
          description: Conflict - Order already exists for this session ID (race condition).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /stripe/payment-intent:
    post:
      tags:
        - Stripe Webhooks
      summary: Handle Stripe Payment Intent Events
      description: Webhook endpoint to receive Payment Intent events from Stripe (`created`, `succeeded`, `processing`, `canceled`, `failed`). Updates payment status in the database. Verifies Stripe signature (assumed handled by middleware or infrastructure). Ignores events not matching the configured `STRIPE_ENV`.
      requestBody:
        required: true
        description: Stripe Event object (Payment Intent related types).
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StripeEvent' # Generic Stripe event, specific type checked internally
      responses:
        '200':
          description: Event received and processed successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: success getting checkout-session # Message might vary
        '400':
          $ref: '#/components/responses/BadRequest' # Invalid payload, event type, or Stripe signature validation failure
        '409':
          description: Conflict - Payment already exists in a final state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          $ref: '#/components/responses/InternalServerError'

components:
  schemas:
    # ---------- Pagination ----------
    # Shared admin-list envelope. A list endpoint's `data` is an object shaped as
    # `allOf: [PaginationMeta, { data: <array of entity> }]`, i.e.
    # `{ data: [...], page, pageSize, total }`.
    PaginationMeta:
      type: object
      properties:
        page:
          type: integer
          example: 1
        pageSize:
          type: integer
          example: 10
        total:
          type: integer
          description: Total rows across all pages.
          example: 137
    # ---------- Basic Entities ----------
    ErrorResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            type: string
          example: ["Invalid username or password"]
    SuccessData: # Often includes accessToken
      type: object
      description: Placeholder for successful response data structure.
      properties:
        accessToken:
          type: string
          description: JWT access token (present in most authenticated responses).
    SuccessResponse: # Basic success structure
      type: object
      properties:
        message:
          type: string
          description: A human-readable message indicating the result of the operation.
        data: # Optional data payload
          nullable: true
          oneOf:
            - type: object
            - type: array
            - type: string
            - type: number
            - type: boolean
    SecretKeyInput:
      type: object
      properties:
        secretKey:
          type: string
          description: Secret key required for authorization.
      required:
        - secretKey

    # --- Country/Currency/Tax ---
    ClientCurrency:
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          example: EUR
          pattern: '^[A-Z]{3}$'
        symbol:
          type: string
          example: €
        stripeName:
          type: string
          example: eur
    HealthStatus: # Readiness probe payload
      type: object
      properties:
        status:
          type: string
          enum: [ok, degraded]
        checks:
          type: object
          properties:
            d1:
              type: string
              enum: [ok, error]
            kv:
              type: string
              enum: [ok, error]
            r2:
              type: string
              enum: [ok, error]
          required: [d1, kv, r2]
      required:
        - status
        - checks

    DashboardSummary: # Operational overview for the backoffice home
      type: object
      properties:
        counts:
          type: object
          properties:
            orders:
              type: integer
            products:
              type: integer
            users:
              type: integer
            collections:
              type: integer
          required: [orders, products, users, collections]
        lowStock:
          type: array
          description: Products at/under the low-stock threshold (bounded).
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              name:
                type: string
              stock:
                type: integer
        recentOrders:
          type: array
          description: Most recent orders (bounded, newest first).
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              createdAt:
                type: string
                format: date-time
              isCanceled:
                type: boolean
      required:
        - counts
        - lowStock
        - recentOrders

    AdminTax: # A per-country tax rate; `id` is the Stripe TaxRate id (txr_…)
      type: object
      properties:
        id:
          type: string
          description: The Stripe TaxRate id (txr_…) — checkout charges via this id.
        name:
          type: string
        rate:
          type: number
          format: float
          description: Fraction (e.g. 0.23 = 23%), as applied to prices.
        countryCode:
          type: string
          pattern: '^[A-Z]{2}$'
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    CreateTaxInput: # Body for POST /private/taxes
      type: object
      required:
        - name
        - countryCode
        - rate
      properties:
        name:
          type: string
        countryCode:
          type: string
          pattern: '^[A-Z]{2}$'
        rate:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Fraction (0.23 = 23%). Stripe receives the percentage.
        inclusive:
          type: boolean
          description: Whether the tax is included in the price (default false).
        description:
          type: string
    UpdateTaxInput: # Body for PUT /private/taxes/{id} — rate & country are immutable
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        active:
          type: boolean
          description: Set false to deactivate the Stripe rate.

    Currency: # Full currency details including rate
      allOf:
        - $ref: '#/components/schemas/ClientCurrency'
        - type: object
          properties:
            rateToEuro:
              type: number
              format: float
              description: Exchange rate relative to EUR (1 EUR = X rateToEuro).
            createdAt:
              type: string
              format: date-time
            lastModifiedAt:
              type: string
              format: date-time
    InsertCurrency: # Schema for POST/PUT /private/currencies
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          example: USD
          pattern: '^[A-Z]{3}$'
        symbol:
          type: string
          example: $
        rateToEuro:
          type: number
          format: float
        stripeName:
          type: string
          example: usd
      required:
        - name
        - code
        - symbol
        - rateToEuro
        - stripeName
    ClientTax:
      type: object
      properties:
        name:
          type: string
        rate:
          type: number
          format: float # e.g., 0.23 for 23%
    Tax: # Full tax details including Stripe ID
      allOf:
        - $ref: '#/components/schemas/ClientTax'
        - type: object
          properties:
            id:
              type: string
              description: Stripe Tax Rate ID.
              example: txr_...
            createdAt:
              type: string
              format: date-time
            lastModifiedAt:
              type: string
              format: date-time
    ExtendedClientTax: # Tax with calculated amount
      allOf:
        - $ref: '#/components/schemas/ClientTax'
        - type: object
          properties:
            amount:
              type: integer
              format: int32
              description: Calculated tax amount in cents for a given price.
    ClientCountry: # Used for public listing
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          example: PT
          pattern: '^[A-Z]{2}$'
        locale:
          type: string
          example: pt-PT
        currency:
          $ref: '#/components/schemas/ClientCurrency'
        taxes:
          type: array
          items:
            $ref: '#/components/schemas/ClientTax'
    Country: # Full country details including full Tax info and dates
      allOf:
        - $ref: '#/components/schemas/ClientCountry'
        - type: object
          properties:
            taxes: # Override with full Tax schema
              type: array
              items:
                $ref: '#/components/schemas/Tax'
            createdAt:
              type: string
              format: date-time
            lastModifiedAt:
              type: string
              format: date-time
    InsertCountry: # Schema for POST/PUT /private/countries
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          pattern: '^[A-Z]{2}$'
        locale:
          type: string
        currencyCode:
          type: string
          pattern: '^[A-Z]{3}$'
      required:
        - name
        - code
        - locale
        - currencyCode

    # --- Image ---
    ImageDTO: # Basic image info for lists/previews
      type: object
      properties:
        url:
          type: string
          format: url
        alt:
          type: string
    Image: # Full image details including dates
      allOf:
        - $ref: '#/components/schemas/ImageDTO'
        - type: object
          properties:
            productId:
              type: string
              format: uuid
              nullable: true
            collectionId:
              type: string
              format: uuid
              nullable: true
            isThumbnail:
              type: boolean
            createdAt:
              type: string
              format: date-time
            lastModifiedAt:
              type: string
              format: date-time

    ImageUploadResponseData: # The hosted URL returned by POST /private/images/upload
      type: object
      properties:
        url:
          type: string
          format: url
          example: https://images.invernspirit.com/6f1c2b7a-....png

    CreateImageInput: # Body for POST /private/images — register an uploaded URL as a record
      type: object
      required:
        - url
      properties:
        url:
          type: string
          format: url
          description: The hosted image URL returned by the upload endpoint.
        alt:
          type: string
        productId:
          type: string
          format: uuid
        collectionId:
          type: string
          format: uuid
        isThumbnail:
          type: boolean

    UpdateImageInput: # Body for PUT /private/images — url identifies the record, the rest are editable
      type: object
      required:
        - url
      properties:
        url:
          type: string
          format: url
          description: Identifies the image record to update (its primary key).
        alt:
          type: string
        productId:
          type: string
          format: uuid
        collectionId:
          type: string
          format: uuid
        isThumbnail:
          type: boolean

    # --- Product/Collection ---
    Product: # Core product data
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        priceInCents:
          type: integer
          format: int32
          description: Net price before tax.
        stock:
          type: integer
          format: int32
        weight:
          type: integer
          format: int32
          description: Weight in grams.
        images: # Array for list views, often limited to 1
          type: array
          items:
            $ref: '#/components/schemas/ImageDTO'
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    InsertProduct: # Create body for POST /private/products (stock is set on create)
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        priceInCents:
          type: integer
          format: int32
        stock:
          type: integer
          format: int32
        weight:
          type: integer
          format: int32
        collectionId:
          type: string
          format: uuid
      required:
        - name
        - description
        - priceInCents
        - stock
        - weight
        - collectionId
    UpdateProduct: # Update body for PUT /private/products/{id}
      description: >-
        Same as InsertProduct **without `stock`**. Stock is not editable via a
        product update — it lives in three stores (D1/KV/R2) and must change only
        through the dedicated stock write-through path. Any `stock` in the body is
        ignored.
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        priceInCents:
          type: integer
          format: int32
        weight:
          type: integer
          format: int32
        collectionId:
          type: string
          format: uuid
      required:
        - name
        - description
        - priceInCents
        - weight
        - collectionId
    ExtendedProduct: # Product with calculated prices and taxes
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            netPrice:
              type: integer
              format: int32
              description: Price before tax (same as priceInCents).
            grossPrice:
              type: integer
              format: int32
              description: Price including tax.
            taxes:
              type: array
              items:
                $ref: '#/components/schemas/ExtendedClientTax'
          required:
            - netPrice
            - grossPrice
            - taxes
    ProductDetails: # Product with full details (description, all images)
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            description:
              type: string
            images: # Override Product images to use full Image schema array
              type: array
              items:
                $ref: '#/components/schemas/Image'
            collectionId:
              type: string
              format: uuid
      required:
        - description
        - collectionId
    ProductWithCollectionDetails: # Product details including linked collection name/ID
      allOf:
        - $ref: '#/components/schemas/ProductDetails'
        - type: object
          properties:
            collection:
              type: object
              properties:
                id:
                  type: string
                  format: uuid
                name:
                  type: string
              required:
                - id
                - name
          required:
            - collection
    ExtendedProductWithCollectionDetails: # Full details + calculated prices/taxes
      allOf:
        - $ref: '#/components/schemas/ProductWithCollectionDetails'
        - $ref: '#/components/schemas/ExtendedProduct' # Inherit pricing fields
      required: # Ensure inherited required fields are listed if needed
        - netPrice
        - grossPrice
        - taxes
    Collection: # Collection info for lists
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        image:
          $ref: '#/components/schemas/Image' # Only one image shown in list view
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    InsertCollection: # Schema for POST/PUT /private/collections
      type: object
      properties:
        name:
          type: string
        description:
          type: string
      required:
        - name
        - description
    CollectionDetails: # Full collection details with basic products
      allOf:
        - $ref: '#/components/schemas/Collection'
        - type: object
          properties:
            description:
              type: string
            products:
              type: array
              items:
                $ref: '#/components/schemas/Product'
      required:
        - description
        - products
    ExtendedCollectionDetails: # Full collection details with extended products (prices/taxes)
      allOf:
        - $ref: '#/components/schemas/CollectionDetails'
        - type: object
          properties:
            products: # Override products with extended version
              type: array
              items:
                $ref: '#/components/schemas/ExtendedProduct'
      required:
        - products

    # --- Cart ---
    LineItem: # Product within a cart context
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            quantity:
              type: integer
              format: int32
              minimum: 1 # Usually, items in cart have quantity >= 1
          required:
            - quantity
    LineItemError:
      type: object
      properties:
        message:
          type: string
        type:
          type: string
          enum: [NOT_ENOUGH_STOCK]
      required:
        - message
        - type
    ExtendedLineItem: # Line item with calculated prices/taxes and potential issues
      allOf:
        - $ref: '#/components/schemas/LineItem'
        - $ref: '#/components/schemas/ExtendedProduct' # Inherit pricing fields
        - type: object
          properties:
            issues:
              type: array
              items:
                $ref: '#/components/schemas/LineItemError'
      required: # Ensure inherited required fields are listed
        - netPrice
        - grossPrice
        - taxes
    CartDTO: # Basic structure of items in a cart
      type: object
      properties:
        products:
          type: array
          items:
            $ref: '#/components/schemas/LineItem'
    Cart: # Full Cart object including metadata
      type: object
      properties:
        id:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
        isLoggedIn:
          type: boolean
        products:
          type: array
          items:
            $ref: '#/components/schemas/LineItem'
    ExtendedCart: # Cart with calculated totals, taxes, and checkout status
      allOf:
        - $ref: '#/components/schemas/CartDTO'
        - type: object
          properties:
            products: # Override with extended line items
              type: array
              items:
                $ref: '#/components/schemas/ExtendedLineItem'
            netPrice:
              type: integer
              format: int32
              description: Total net price of all items in the cart.
            grossPrice:
              type: integer
              format: int32
              description: Total gross price of all items in the cart (including taxes).
            taxes: # Aggregated taxes for the whole cart
              type: array
              items:
                $ref: '#/components/schemas/ExtendedClientTax'
            isCheckoutPossible:
              type: boolean
              description: Indicates if checkout can proceed (e.g., cart not empty, no stock issues).
            issues:
              type: array
              items:
                type: string
              description: List of issues preventing checkout (e.g., 'Some products have issues').
      required:
        - products
        - netPrice
        - grossPrice
        - taxes
        - isCheckoutPossible
    CartItemQuantityInputPut: # For PUT /cart/items/{id}
      type: object
      properties:
        quantity:
          type: integer
          format: int32
          minimum: 0 # PUT sets the exact quantity, 0 means remove.
          description: The exact quantity to set for the item. 0 removes the item.
      required:
        - quantity
    CartResponse: # Standard response format for cart operations
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            cart:
              $ref: '#/components/schemas/ExtendedCart'
            accessToken: # May include refreshed anonymous or logged-in token
              type: string
      required:
        - message
        - data

    # --- User / Address ---
    AddressInput: # Structure for providing address data
      type: object
      description: Address structure for input. Country is determined by the path parameter.
      properties:
        street:
          type: string
        houseNumber:
          type: string
        apartment:
          type: string
          nullable: true
        postalCode:
          type: string
        city:
          type: string
        province:
          type: string
          nullable: true
      required:
        - street
        - houseNumber
        - postalCode
        - city
    Address: # Full address structure including country code
      allOf:
        - $ref: '#/components/schemas/AddressInput'
        - type: object
          properties:
            country:
              type: string
              description: ISO 2-letter country code.
              example: PT
              pattern: '^[A-Z]{2}$'
          required:
            - country
    UserDetailsInput: # Structure for providing user details (guest checkout)
      type: object
      properties:
        email:
          type: string
          format: email
        firstName:
          type: string
        lastName:
          type: string
          nullable: true
      required:
        - email
        - firstName
    UserDetails: # User details without address
      allOf:
        - $ref: '#/components/schemas/UserDetailsInput'
    UpdateAdminOrderInput: # Constrained admin order update (PUT /private/orders/{id})
      description: >-
        The only fields an admin may correct on an existing order. Financial /
        immutable fields (stripeId, paymentId, products, totals, userId,
        shippingTransactionId, isCanceled) are NOT accepted — cancellation goes
        through /cancel and fulfillment through /fulfillment. Unknown keys are
        ignored. At least one field must be present.
      type: object
      minProperties: 1
      properties:
        address:
          $ref: '#/components/schemas/Address'
        personalDetails:
          $ref: '#/components/schemas/UserDetails'
    FulfillmentInput: # Update an order's fulfillment (PUT /private/orders/{id}/fulfillment)
      description: Provide `status` and/or `trackingUrl` (at least one).
      type: object
      minProperties: 1
      properties:
        status:
          type: string
          enum: [processing, shipped, delivered, canceled]
          description: >-
            Must be a valid transition from the current status
            (processing -> shipped -> delivered; -> canceled from
            processing/shipped). Illegal jumps return 409.
        trackingUrl:
          type: string
          format: uri
          nullable: true
          description: Customer-visible; must be an https URL.
          example: https://track.example.com/xyz
    UserDTO: # Data returned about the user (excluding sensitive info like password, role, internal IDs)
      allOf:
        - $ref: '#/components/schemas/UserDetails'
        - type: object
          properties:
            address:
              $ref: '#/components/schemas/Address'
              nullable: true
    BaseUser: # Full user schema as stored in DB (excluding password hash)
      allOf:
        - $ref: '#/components/schemas/UserDTO'
        - type: object
          properties:
            id:
              type: string
              format: uuid
            createdAt:
              type: string
              format: date-time
            lastModifiedAt:
              type: string
              format: date-time
            version:
              type: integer
            role:
              type: string
              enum: [ADMIN, USER]
            isOauth:
              type: boolean
            googleUserId:
              type: string
              nullable: true
            isValidated:
              type: boolean
      required:
        - id
        - createdAt
        - lastModifiedAt
        - version
        - role
        - isOauth
        - isValidated
    AdminUser: # Safe admin projection — role included, never password or google id
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        firstName:
          type: string
        lastName:
          type: string
          nullable: true
        role:
          type: string
          enum: [ADMIN, USER]
        isOauth:
          type: boolean
        isValidated:
          type: boolean
        disabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
      required:
        - id
        - email
        - firstName
        - role
        - isOauth
        - isValidated
        - disabled
    UpdateUserInput: # Body for PUT /private/users/{id} — whitelisted admin fields
      type: object
      properties:
        role:
          type: string
          enum: [ADMIN, USER]
        isValidated:
          type: boolean
        disabled:
          type: boolean
    GetUserResponse:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/UserDTO'
            accessToken:
              type: string
      required:
        - message
        - data
    UpdateUserResponse: # Response after successful user update
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/UserDTO'
            accessToken:
              type: string
      required:
        - message
        - data
    DeleteUserResponse:
      type: object
      properties:
        message:
          type: string
        data: # Contains anonymous token
          $ref: '#/components/schemas/SuccessData'
      required:
        - message
        - data
    UpdatePersonalInformationInput:
      type: object
      description: Fields to update personal information. At least one property (firstName, lastName, or address) must be provided.
      properties:
        firstName:
          type: string
        lastName:
          type: string
        address:
          $ref: '#/components/schemas/AddressInput'
      minProperties: 1
    UpdatePasswordInput:
      type: object
      properties:
        currentPassword:
          type: string
          format: password
        newPassword:
          type: string
          format: password
      required:
        - currentPassword
        - newPassword
    SubmitEmailChangeInput:
      type: object
      properties:
        email:
          type: string
          format: email
          description: The new email address to change to.
      required:
        - email
    ValidateEmailChangeCodeInput:
      type: object
      properties:
        code:
          type: string
          description: The validation code received in the new email.
      required:
        - code

    # --- Auth / Config Responses ---
    AuthSuccessResponse: # Response for successful login/signup validation
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/UserDTO'
            cart:
              $ref: '#/components/schemas/ExtendedCart'
            accessToken:
              type: string
          required:
            - user
            - cart
            - accessToken
    LogoutResponse: # Response for successful logout
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            cart: # Represents an empty cart state after logout
              $ref: '#/components/schemas/ExtendedCart'
            accessToken: # New anonymous token
              type: string
          required:
            - cart
            - accessToken
    SignupResponse: # Response for initiating signup (before email validation)
      type: object
      properties:
        message:
          type: string
        data: # Contains anonymous token
          $ref: '#/components/schemas/SuccessData'
      required:
        - message
        - data
    ConfigResponseLoggedIn: # Response for /config when user is logged in
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/UserDTO'
            cart:
              $ref: '#/components/schemas/ExtendedCart'
            accessToken:
              type: string
          required:
            - user
            - cart
            - accessToken
    ConfigResponseLoggedOut: # Response for /config when user is logged out/anonymous
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            cart:
              $ref: '#/components/schemas/ExtendedCart' # Could be empty or existing anonymous cart
            accessToken: # Anonymous token
              type: string
          required:
            - cart
            - accessToken
    SignupInput:
      type: object
      properties:
        firstName:
          type: string
        lastName:
          type: string
          nullable: true
        email:
          type: string
          format: email
        password:
          type: string
          format: password
        remember:
          type: boolean
          default: false
      required:
        - firstName
        - email
        - password
    VerifyEmailInput:
      type: object
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          description: The 8-digit verification code from the signup email.
      required:
        - email
        - code
    ResendEmailInput:
      type: object
      properties:
        email:
          type: string
          format: email
      required:
        - email
    LoginInput:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
        remember:
          type: boolean
          default: false
      required:
        - email
        - password
    SubmitForgotPasswordEmailInput:
      type: object
      properties:
        email:
          type: string
          format: email
      required:
        - email
    ValidateCodeInput:
      type: object
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          description: The 8-digit verification code.
      required:
        - email
        - code
    ResetPasswordInput:
      type: object
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          description: The 8-digit verification code.
        password:
          type: string
          format: password
          description: The new password for the user.
      required:
        - email
        - code
        - password

    # ---------- Checkout Schemas ----------
    ClientCheckoutStage: # Structure for stages returned by /checkout/stages GET
      type: object
      properties:
        name:
          type: string
          enum: [personal-details, address, shipping, review]
        isEnabled:
          type: boolean
        title:
          type: string
      required:
        - name
        - isEnabled
        - title
    CheckoutStagesResponse: # Response for GET /checkout/stages
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
            isCheckoutPossible: # Optional, present if stages cannot be determined due to cart issues
              type: boolean
              description: Indicates if checkout is generally possible (e.g., cart not empty).
      required:
        - message
        - data
    CheckoutStageErrorResponse: # Specific error when a stage is invalid/not enabled
      type: object
      properties:
        message:
          type: string
          example: Address checkout stage is not enabled
        data:
          type: object
          properties:
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageInputPersonalDetails:
      type: object
      properties:
        personalDetails:
          $ref: '#/components/schemas/UserDetailsInput'
      required:
        - personalDetails
    CheckoutStageResponsePersonalDetailsGet:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            personalDetails:
              $ref: '#/components/schemas/UserDetails'
              nullable: true # Can be null if not set yet
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageResponsePersonalDetailsPost:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            personalDetails:
              $ref: '#/components/schemas/UserDetails'
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageInputAddress:
      type: object
      properties:
        address:
          $ref: '#/components/schemas/AddressInput'
        saveAddress:
          type: boolean
          description: If true and user is logged in, save address to profile.
          default: false
      required:
        - address
    CheckoutStageResponseAddressGet:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            address:
              $ref: '#/components/schemas/Address'
              nullable: true
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageResponseAddressPost:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            address:
              $ref: '#/components/schemas/Address'
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageInputShipping:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: The ID of the *shipping method* to select.
      required:
        - id
    CheckoutStageResponseShippingGet:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            shippingMethods:
              type: array
              items:
                $ref: '#/components/schemas/SelectedShippingMethod'
            selectedShippingMethod:
              $ref: '#/components/schemas/SelectedShippingMethod'
              nullable: true
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageResponseShippingPost:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            shippingMethod:
              $ref: '#/components/schemas/SelectedShippingMethod'
            availableCheckoutStages:
              type: array
              items:
                $ref: '#/components/schemas/ClientCheckoutStage'
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageReviewItem: # Structure for review items (with edit status)
      properties:
        isEditable:
          type: boolean
      required:
        - isEditable
    CheckoutStageResponseReviewGet:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            personalDetails:
              allOf:
                - $ref: '#/components/schemas/UserDetails'
                - $ref: '#/components/schemas/CheckoutStageReviewItem'
            address:
              allOf:
                - $ref: '#/components/schemas/Address'
                - $ref: '#/components/schemas/CheckoutStageReviewItem'
            shippingMethod:
              allOf:
                - $ref: '#/components/schemas/SelectedShippingMethod'
                - $ref: '#/components/schemas/CheckoutStageReviewItem'
            cart:
              $ref: '#/components/schemas/ExtendedCart'
            totalPrice:
              type: integer
              format: int32
              description: Total price including items, taxes, and shipping (in cents).
            accessToken:
              type: string
      required:
        - message
        - data
    CheckoutStageResponsePaymentGet:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            url:
              type: string
              format: url
              description: The URL to redirect the user to Stripe Checkout.
            accessToken:
              type: string
      required:
        - message
        - data

    # --- Shipping ---
    ShippingRate:
      type: object
      properties:
        id:
          type: string
          format: uuid
        priceInCents:
          type: integer
          format: int32
        minWeight:
          type: integer
          format: int32
        maxWeight:
          type: integer
          format: int32
        deliveryTime:
          type: integer
          format: int32
          description: Estimated delivery time in business days.
        countryCodes:
          type: array
          items:
            type: string
            example: PT
            pattern: '^[A-Z]{2}$'
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    BaseShippingMethod:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    SelectedShippingMethod: # Shipping method with the applicable rate selected
      allOf:
        - $ref: '#/components/schemas/BaseShippingMethod'
        - type: object
          properties:
            rate:
              $ref: '#/components/schemas/ShippingRate'
      required:
        - rate

    ShippingMethod: # Admin shipping method with all of its rates
      allOf:
        - $ref: '#/components/schemas/BaseShippingMethod'
        - type: object
          properties:
            rates:
              type: array
              items:
                $ref: '#/components/schemas/ShippingRate'
      required:
        - rates
    InsertShippingMethod: # Create/update body for a shipping method
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          example: Express Air
    InsertShippingRate: # Create/update body for a rate (parent method from the path)
      type: object
      required: [priceInCents, minWeight, maxWeight, deliveryTime]
      properties:
        priceInCents:
          type: integer
          format: int32
          minimum: 0
          example: 500
        minWeight:
          type: integer
          format: int32
          minimum: 0
          description: Inclusive lower bound of the half-open weight band [min, max).
        maxWeight:
          type: integer
          format: int32
          minimum: 0
          description: Exclusive upper bound of the half-open weight band [min, max).
        deliveryTime:
          type: integer
          format: int32
          minimum: 0
          description: Estimated delivery time in business days.
    RateCountriesInput: # Replace-set of countries a rate applies to
      type: object
      required: [countryCodes]
      properties:
        countryCodes:
          type: array
          items:
            type: string
            pattern: '^[A-Z]{2}$'
            example: PT

    # --- Payment / Order ---
    PaymentMethod: # Info about the payment method used
      type: object
      properties:
        type:
          type: string
          enum: [card, paypal]
        brand:
          type: string
          nullable: true
          example: visa
        last4:
          type: string
          nullable: true
          example: '4242'
    ClientPayment: # Payment details shown to the client
      type: object
      properties:
        state:
          type: string
          enum: [draft, succeeded, canceled, created, processing, failed]
        netAmount:
          type: integer
          format: int32
          description: Amount before taxes (might be null if not applicable/available).
          nullable: true
        grossAmount:
          type: integer
          format: int32
          description: Total amount paid.
        paymentMethod:
          $ref: '#/components/schemas/PaymentMethod'
          nullable: true
    ShippingTransaction:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [processing, shipped, delivered, canceled]
        trackingUrl:
          type: string
          format: url
          nullable: true
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
    ClientOrder: # Order structure returned to the client
      type: object
      properties:
        id:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        lastModifiedAt:
          type: string
          format: date-time
        products:
          type: array
          items:
            $ref: '#/components/schemas/LineItem' # Basic line item info
        address:
          $ref: '#/components/schemas/Address'
        personalDetails:
          $ref: '#/components/schemas/UserDetails'
        shippingMethod:
          $ref: '#/components/schemas/SelectedShippingMethod'
        payment:
          $ref: '#/components/schemas/ClientPayment'
          nullable: true
        shippingTransaction:
          $ref: '#/components/schemas/ShippingTransaction'
        isCanceled:
          type: boolean
          nullable: true # Might be null before explicitly set
      required:
        - id
        - createdAt
        - lastModifiedAt
        - products
        - address
        - personalDetails
        - shippingMethod
        - shippingTransaction
    ExtendedClientOrder: # Order structure with calculated fields (taxes, totals, status)
      allOf:
        - $ref: '#/components/schemas/ClientOrder'
        - type: object
          properties:
            products: # Override with extended items including calculated prices/taxes
              type: array
              items:
                $ref: '#/components/schemas/ExtendedLineItem'
            taxes: # Aggregated taxes for the order
              type: array
              items:
                $ref: '#/components/schemas/ExtendedClientTax'
            currency:
              $ref: '#/components/schemas/ClientCurrency'
            status: # Calculated overall order status
              type: string
              enum: [processing_payment, packaging, shipping, completed, canceled, error]
              description: Calculated order status based on payment and shipping states.
      required:
        - products
        - taxes
        - currency
        - status
    Order: # Full internal order representation (includes internal IDs like userId, paymentId, etc.)
      allOf:
        - $ref: '#/components/schemas/ClientOrder' # Base client view
        - type: object
          properties:
            userId:
              type: string
              format: uuid
              nullable: true
            paymentId:
              type: string
              format: uuid # Assuming internal payment ID is UUID
              nullable: true
            stripeId: # The Stripe Checkout Session ID
              type: string
              example: cs_...
            isCanceled:
              type: boolean
              nullable: true # Explicitly defining as nullable
      required:
        - stripeId
    GetUserOrdersResponse:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            orders:
              type: array
              items:
                $ref: '#/components/schemas/ExtendedClientOrder'
            accessToken:
              type: string
      required:
        - message
        - data
    GetOrderResponse:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            order:
              $ref: '#/components/schemas/ExtendedClientOrder'
            accessToken:
              type: string
      required:
        - message
        - data

    # ---------- OAuth Schemas ----------
    OAuthRedirectResponse:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            url:
              type: string
              format: url
              description: URL to redirect the user to Google.
      required:
        - message
        - data
    OAuthCallbackResponse:
      type: object
      properties:
        message:
          type: string
        data:
          type: object
          properties:
            accessToken:
              type: string
      required:
        - message
        - data

    # ---------- Stripe Schemas ----------
    StripeEvent:
      type: object
      description: Generic structure for a Stripe webhook event. The `data.object` type varies.
      properties:
        id:
          type: string
          description: Stripe event ID.
        object:
          type: string
          enum: [event]
        api_version:
          type: string
        created:
          type: integer
          format: int64
          description: Timestamp of event creation (Unix epoch seconds).
        data:
          type: object
          properties:
            object:
              type: object
              description: The actual Stripe object related to the event (e.g., PaymentIntent, CheckoutSession). Varies by event type.
        livemode:
          type: boolean
        pending_webhooks:
          type: integer
        request:
          type: object
          nullable: true
          properties:
            id:
              type: string
              nullable: true
            idempotency_key:
              type: string
              nullable: true
        type:
          type: string
          description: The type of event (e.g., 'checkout.session.completed', 'payment_intent.succeeded').
      required:
        - id
        - object
        - created
        - data
        - livemode
        - type

  securitySchemes:
    AdminBearer: # Real admin auth: JWT access token + refresh cookie
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Admin access token (JWT) sent as `Authorization: Bearer <token>`. The
        token must carry `role: ADMIN`, enforced by
        `apps/backend/src/middleware/require-admin.ts`. The refresh-token cookie (`s_r`)
        must also be present on the request — `getCredentials` requires both the
        access token and the refresh token. Obtain both by logging in
        (`POST /public/countries/{countryCode}/user/login`) as an ADMIN user.
        The maintenance route `stock/setup` instead validates a `secretKey`
        field in its request body and is not covered by this scheme.

  responses:
    BadRequest:
      description: Bad Request - Invalid input data, parameters, or request format. Check `issues` for details.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Unauthorized - Authentication required or invalid credentials/token/secret key.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Forbidden - Access denied, insufficient permissions, or operation not allowed in current state.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Not Found - The requested resource could not be found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Conflict - The request could not be completed due to a conflict with the current state of the resource (e.g., email already exists, order already processed).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InternalServerError:
      description: Internal Server Error - An unexpected error occurred on the server.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CheckoutStageNotEnabled:
      description: Bad Request - The requested checkout stage is not currently enabled or accessible.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/CheckoutStageErrorResponse'
    CheckoutStageNotEnabledOrBadRequest: # Combines stage errors with other potential 400s
      description: Bad Request - Either the stage is not enabled, or the input data is invalid (e.g., address mismatch, invalid rate). See `issues` for details.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/CheckoutStageErrorResponse'
              - $ref: '#/components/schemas/ErrorResponse'

  parameters:
    PageParam:
      name: page
      in: query
      required: false
      description: 1-based page number for admin list endpoints (default 1).
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSizeParam:
      name: pageSize
      in: query
      required: false
      description: Items per page for admin list endpoints (default 10, max 100).
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 10
    SortOrderParam:
      name: sortOrder
      in: query
      required: false
      description: >-
        Sort direction for admin list endpoints. Only applied when `sortBy` is
        set. Defaults to ascending.
      schema:
        type: string
        enum: [asc, desc]
        default: asc
    CountryCode:
      name: countryCode
      in: path
      required: true
      description: ISO 2-letter country code (uppercase, e.g., PT, ES).
      schema:
        type: string
        pattern: '^[A-Z]{2}$'
        example: PT
    ProductId:
      name: id
      in: path
      required: true
      description: The unique identifier (UUID) of the product.
      schema:
        type: string
        format: uuid
        example: nPn6WYEMz1NK2B61hmrWsb
    CollectionId:
      name: id
      in: path
      required: true
      description: The unique identifier (UUID) of the collection.
      schema:
        type: string
        format: uuid
        example: 9bekuxMKGrRTKvZqNYZdDV
    CartId: # Added
      name: id
      in: path
      required: true
      description: The unique identifier (UUID) of the cart.
      schema:
        type: string
        format: uuid
    UserId: # Added
      name: id
      in: path
      required: true
      description: The unique identifier (UUID) of the user.
      schema:
        type: string
        format: uuid
    OrderId:
      name: id
      in: path
      required: true
      description: The unique identifier (UUID) of the order.
      schema:
        type: string
        format: uuid
        example: w3ChBD7SjeReRrNSwSSsa3
    SearchQuery: # Added
      name: search
      in: query
      required: false
      description: Optional search term to filter results by name or description.
      schema:
        type: string
    PageQuery: # Added
      name: page
      in: query
      required: false
      description: Page number for pagination (default 1).
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 1
    PageSizeQuery: # Added
      name: pageSize
      in: query
      required: false
      description: Number of items per page (default 10).
      schema:
        type: integer
        format: int32
        minimum: 1
        maximum: 100 # Example max, adjust as needed
        default: 10

  headers:
    SetCookieAuth: # For Login, Signup Validation, OAuth Callback
      description: Sets `s_r` (refresh token, HttpOnly, Secure, SameSite=Strict) and optionally `r_m` (remember me, Secure, SameSite=Strict) if remember=true. Deletes `c_i`, `a_d`, `u_d`, `s_m`, `c_s`, `c_m`.
      schema:
        type: string
    SetCookieAuthAndOAuthDelete: # For OAuth Callback specifically
      description: Sets `s_r` and `r_m`. Deletes `o_t`, `c_i`, `a_d`, `u_d`, `s_m`, `c_s`, `c_m`.
      schema:
        type: string
    SetCookieLogout: # For Logout, Delete User
      description: Deletes `s_r` and `r_m`. Sets a new anonymous `s_r` token. Deletes checkout cookies (`a_d`, `u_d`, `s_m`, `c_s`, `c_m`).
      schema:
        type: string
    SetCookieAnonymous: # For Signup Init, Forgot Password Submit/Reset/Validate
      description: Sets a new anonymous `s_r` token. May delete other cookies like `c_i`, checkout cookies.
      schema:
        type: string
    SetCookieOptional: # For general authenticated endpoints GET /config, GET /cart, etc.
      description: May refresh the `s_r` cookie (refresh token) and/or `c_i` cookie (cart ID).
      schema:
        type: string
    SetCookieOptionalOrCustomerEmail: # For GET /orders/{id}
      description: May refresh `s_r` cookie. May set `c_m` (encrypted customer email) if guest lookup via header was successful.
      schema:
        type: string
    SetCookiePersonalDetails: # For POST /checkout/stages/personal-details
      description: Sets `u_d` (encrypted user details) cookie. May refresh `s_r`.
      schema:
        type: string
    SetCookieAddress: # For POST /checkout/stages/address
      description: Sets `a_d` (encrypted address) cookie. May refresh `s_r`.
      schema:
        type: string
    SetCookieShipping: # For POST /checkout/stages/shipping
      description: Sets `s_m` (encrypted shipping method ID) cookie. May refresh `s_r`.
      schema:
        type: string
    SetCookieCheckoutSession: # For GET /checkout/stages/payment
      description: Sets `c_s` (encrypted Stripe checkout session ID) cookie. May refresh `s_r`.
      schema:
        type: string
    SetCookieOAuthToken: # For GET /oauth/google/redirect
      description: Sets `o_t` (OAuth state token) cookie (HttpOnly, Secure, SameSite=Lax).
      schema:
        type: string